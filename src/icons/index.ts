/**
 * @fhe/brand · icons barrel
 * Single Icon component + tree-shakable name registry.
 *
 *   import { Icon } from "@fhe/brand";
 *   <Icon name="shieldcheck" size={20} />
 *
 * For the registry of available names, see ./paths.tsx.
 */
export { Icon, type IconProps } from "./icon";
export { iconNames, type IconName } from "./paths";
